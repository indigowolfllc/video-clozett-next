name: Clean Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Slack Test
        run: |
          curl -X POST \
          -H "Content-type: application/json" \
          --data '{"text":"Clean workflow working 🚀"}' \
          ${{ secrets.SLACK_WEBHOOK_URL }}
