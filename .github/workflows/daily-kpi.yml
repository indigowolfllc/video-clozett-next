name: Test Supabase + Slack

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:

      - name: Send Test to Slack
        run: |
          curl -X POST \
          -H "Content-type: application/json" \
          --data '{"text":"Workflow is alive 🚀"}' \
          ${{ secrets.SLACK_WEBHOOK_URL }}
