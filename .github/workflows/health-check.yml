name: Production Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Check Production URL
        id: check
        run: |
          URL="https://video-clozett-next.vercel.app/api/health"
          MAX_TIME=3.0

          echo "Checking: $URL"

          RESPONSE=$(curl -o /dev/null -s -w "%{http_code} %{time_total}" --max-time 15 $URL || echo "000 0")
          STATUS=$(echo $RESPONSE | awk '{print $1}')
          TIME=$(echo $RESPONSE | awk '{print $2}')

          echo "HTTP Status: $STATUS"
          echo "Response Time: ${TIME}s"

          echo "status=$STATUS" >> $GITHUB_OUTPUT
          echo "time=$TIME" >> $GITHUB_OUTPUT

          # ステータスチェック
          if [ "$STATUS" != "200" ]; then
            echo "HTTP failure detected"
            exit 1
          fi

          # パフォーマンスチェック
          awk -v t="$TIME" -v max="$MAX_TIME" 'BEGIN { if (t > max) exit 1 }'

      - name: Notify Slack on Failure
        if: failure()
        run: |
          STATUS="${{ steps.check.outputs.status }}"
          TIME="${{ steps.check.outputs.time }}"
          URL="https://video-clozett-next.vercel.app/api/health"

          if [[ "$STATUS" == 5* ]]; then
            LEVEL="🚨 CRITICAL (Server Error)"
          elif [[ "$STATUS" == 4* ]]; then
            LEVEL="⚠️ WARNING (Client Error)"
          elif [[ "$STATUS" == "200" ]]; then
            LEVEL="⚡ PERFORMANCE ISSUE (Slow Response)"
          else
            LEVEL="❌ UNKNOWN ERROR"
          fi

          curl -X POST -H 'Content-type: application/json' \
          --data "{
            \"text\": \"$LEVEL\nVideo CloZett Production Alert\nStatus: $STATUS\nResponse Time: ${TIME}s\nURL: $URL\"
          }" \
          ${{ secrets.SLACK_WEBHOOK_URL }}
