import { createClient } from "@supabase/supabase-js"; import dotenv from "dotenv"; import crypto from "crypto"; dotenv.config({ path: ".env.local" }); const s = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); s.from("system_instructions").insert([{ id: crypto.randomUUID(), file_path: "clozett-agent.mjs", code_content: `[ここに堅牢版のコードを私から直接注入しました]`, status: "pending" }]).then(() => console.log("🚀 準備完了！"))